# Contributing

This is a solo hobby project. The canonical guidance for humans and coding
agents lives in `AGENTS.md`, with reference documentation under `docs/` and
story canon under `docs/lore/`.

Pull requests follow the review scale in `AGENTS.md` and need green CI. Only
the maintainer merges.
