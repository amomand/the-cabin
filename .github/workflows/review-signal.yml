name: Independent review signal

on:
  pull_request_review:
    types: [submitted, dismissed]

permissions: {}

jobs:
  signal:
    runs-on: ubuntu-latest
    steps:
      - name: Record review event for trusted evaluation
        run: echo "Review event recorded for trusted evaluation."
